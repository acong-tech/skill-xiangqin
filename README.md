# skill-xiangqin

`xiangqin` Claude Code skill 的 ClawHub 分发源。SKILL.md 由主仓
([yarnovo/xiangqin](https://github.com/yarnovo/xiangqin)) 的
`release.yml` workflow 在打 tag 时自动同步过来。

**不要手动改 SKILL.md**——改主仓的然后 `git tag`。
